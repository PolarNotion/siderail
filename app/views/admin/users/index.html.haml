%h1 Users

%table.table
  %thead
    %tr
      %th Email
      %th Created
  %tbody
    - @users.each do |user|
      %tr
        %td
          = user.email
        %td
          = user.created_at
          .small.text-muted= "#{distance_of_time_in_words(user.created_at, Time.now)} ago"
